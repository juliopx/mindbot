# Graphiti Configuration
llm:
  provider: "openai"
  model: "gpt-4o-mini"

embeddings:
  provider: "openai"
  model: "text-embedding-3-small"

storage:
  type: "falkordb"
  url: "falkordb://falkordb:6379"

# Namespacing our memory
group_id: "mindbot-core"
